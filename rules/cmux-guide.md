When running inside cmux (CMUX_SOCKET_PATH is set), use cmux CLI for
workspace management, browser automation, and notifications. Use /cmux
for the full reference. Key commands: identify, tree, new-split, send,
read-screen, browser, notify, set-status, set-progress. Fallback to
tmux if not in cmux (SSH/CI environments).
